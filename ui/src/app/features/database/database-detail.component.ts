import { Component } from '@angular/core';

/** Placeholder component for per-database view (Story 10.5). */
@Component({
  selector: 'app-database-detail',
  standalone: true,
  template: `<p>Database detail — coming in Story 10.5</p>`,
})
export class DatabaseDetailComponent {}
