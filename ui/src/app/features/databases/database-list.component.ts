import { Component } from '@angular/core';

/** Placeholder component for the database list view (Story 10.4). */
@Component({
  selector: 'app-database-list',
  standalone: true,
  template: `<p>Database list — coming in Story 10.4</p>`,
})
export class DatabaseListComponent {}
