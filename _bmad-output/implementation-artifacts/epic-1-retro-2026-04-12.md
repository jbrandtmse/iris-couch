# Epic 1 Retrospective: Project Foundation & Server Identity

**Date:** 2026-04-12
**Epic:** 1 — Project Foundation & Server Identity
**Stories Completed:** 5/5 (100%)
**Total Tests:** 26 passing, 0 failures
**Commits:** 6 (5 stories + 1 hotfix)

---

## Delivery Summary

| Story | Description | Tests | Commit |
|-------|------------|-------|--------|
| 1.1 | Configuration System & Package Scaffold | 4 | `aeb49ef` |
| 1.2 | HTTP Router & CouchDB Welcome Endpoint | 5 | `a9bd50e` |
| 1.3 | UUID Generation Endpoint | 5 | `edd675b` |
| 1.4 | Error Envelope & Consistent Error Responses | 6 | `81acb41` |
| 1.5 | Manual ObjectScript Import Installation | 6 | `3f474a2` |
| — | Hotfix: Router dispatch fix | — | `b177076` |

**Files Created:** 13 ObjectScript classes + module.xml + README updates
**Architecture Packages:** Config, API (Router, ServerHandler), Util (Error, Response, Request, UUID), Test (5 classes), Installer

---

## What Went Well

1. **Consistent test growth with zero regressions** — Tests accumulated 4→9→14→20→26 across stories with no test ever failing after passing. The `OnBeforeOneTest`/`OnAfterOneTest` cleanup pattern worked well.

2. **Architecture patterns held** — Error.Render(), Response.JSON(), handler method signatures, and the Try/Catch pattern were established in Story 1.1-1.2 and followed consistently through all subsequent stories.

3. **Code review auto-resolve was effective** — Reviews caught real issues (catch block patterns, count validation cap, README stale content) and fixed them without blocking progress. Zero critical issues across all 5 reviews.

4. **Testable helper pattern** — Extracting `BuildWelcomeResponse()` as a separate method in Story 1.2 enabled unit testing without HTTP context. This pattern should be reused for all handlers.

5. **Idempotent Installer** — `Install()` and `Uninstall()` both handle already-exists/already-removed gracefully, making setup repeatable and safe.

---

## What Didn't Go Well / Challenges

### 1. Catch Block Pattern Confusion (Recurring)
**What happened:** Story 1.2 had `tSC = ex.AsStatus()` after `Error.Render()` already wrote the response. Fixed in review. Same pattern reappeared in Story 1.4's Router.Page(). 
**Root cause:** The interaction between `Error.Render()` and `%CSP.REST`'s status handling wasn't codified clearly after the first discovery.
**Impact:** Two review cycles spent on the same issue.

### 2. %CSP.REST Page() Override Killed Dispatch (Critical — found in manual testing)
**What happened:** Overriding `Page()` in the Router completely replaced `%CSP.REST`'s dispatch mechanism. All HTTP requests hit the 404 fallback instead of the UrlMap routes. Only discovered during live HTTP testing — unit tests calling handlers directly couldn't detect this.
**Root cause:** `%CSP.REST.Page()` IS the dispatch entry point that calls `DispatchRequest()`. Overriding it removes all routing.
**Fix:** Override `ReportHttpStatusCode` for custom error envelopes instead of `Page()`.

### 3. UrlMap Call Attribute Cross-Class Syntax Doesn't Work (Critical — found in manual testing)
**What happened:** `Call="IRISCouch.API.ServerHandler:HandleWelcome"` in the UrlMap silently failed. Routes were never matched.
**Root cause:** Despite documentation suggesting the `ClassName:Method` syntax works, `%CSP.REST` only supports calling methods on the Router class itself.
**Fix:** Create thin wrapper methods on Router that delegate to handler classes.

### 4. Unit Tests Can't Exercise HTTP Layer
**What happened:** All test classes verify `%DynamicObject` structure but none call `Error.Render()` or `Response.JSON()` against a real `%response` object. Deferred in every code review.
**Root cause:** Unit tests run outside the CSP request context — no `%response` object available.
**Impact:** The two critical bugs above (Page override, Call syntax) were invisible to tests.

### 5. ServerHandler Catch Blocks Don't Use RenderInternal
**What happened:** After Story 1.4 added `RenderInternal()` for NFR-S8-compliant 500 error handling, the existing ServerHandler catch blocks still use `Render()`. Exceptions are silently lost instead of being logged to IRIS console.
**Root cause:** ServerHandler was out of scope for Story 1.4's code review.

---

## Action Items

### Must Do Before Epic 2

| # | Item | Owner | Status |
|---|------|-------|--------|
| 1 | **Fix ServerHandler catch blocks** to use `RenderInternal()` with subsystem-specific reason strings | Story 2.0 | Not started |
| 2 | **Add HTTP integration test approach** — at minimum, use `%Net.HttpRequest` to hit live endpoints and verify responses end-to-end | Story 2.0 | Not started |
| 3 | **Codify Router patterns** in project rules: never override Page(), use local wrapper methods for Call, override ReportHttpStatusCode for custom errors | Done (saved to memory) | Complete |

### Should Do (Track in Deferred Work)

| # | Item | Rationale |
|---|------|-----------|
| 4 | Config.Get() silent failure on invalid keys | Could cause subtle bugs in future epics |
| 5 | Request.ReadBody() no size limit | NFR concern for document storage epic |
| 6 | Metrics dispatch wrapper in Router | Needed for Epic 9 but structure should be ready |

### Lessons Learned (Codified)

| Lesson | Saved To |
|--------|----------|
| After Error.Render(), return $$$OK not ex.AsStatus() | memory: feedback_catch_block_pattern.md |
| Never override Page() in %CSP.REST; Call only supports local methods | memory: feedback_csp_rest_routing.md |
| Always use Perplexity MCP to research ObjectScript before implementing | memory: feedback_perplexity_research.md |

---

## Preparation for Epic 2: Database Lifecycle Management

### Dependencies on Epic 1
- Router + UrlMap pattern (with local wrapper methods) — **ready, pattern verified**
- Error.Render() / ReportHttpStatusCode — **ready**
- Response.JSON() / JSONStatus() — **ready**
- Config.Get() — **ready**
- Request.ReadBody() — **ready**

### New Capabilities Needed
- `IRISCouch.Storage.Database` class for `^IRISCouch.DB` global management
- `IRISCouch.API.DatabaseHandler` for PUT/DELETE/GET `/{db}` routes
- Database name validation (CouchDB naming rules)
- New routes in Router with `:db` path parameter capture

### Story 2.0: Deferred Cleanup
Create Story 2.0 to address:
1. Fix ServerHandler catch blocks → `RenderInternal()`
2. Add HTTP integration test infrastructure
3. Verify Router dispatch works for parameterized routes (`:db`)

### Risks
- **Parameterized routes** (`/:db`) — need to verify `%CSP.REST` regex capture works correctly with our wrapper pattern
- **Global structure design** — `^IRISCouch.DB` is the first real storage global; the pattern set here propagates to all subsequent globals
- **Namespace switching** for database operations that touch `Security.*` classes

---

## Retrospective Status

Epic 1: **done**
Retrospective: **done**
Next: Epic 2 — Database Lifecycle Management
